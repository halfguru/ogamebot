---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - vitest.workspace.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/types/planet.ts
  - packages/shared/src/types/fleet.ts
  - packages/shared/src/types/research.ts
  - packages/shared/src/types/buildings.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/schemas/ogamed.ts
  - packages/shared/src/schemas/planets.ts
  - packages/shared/src/schemas/fleets.ts
  - packages/shared/src/schemas/research.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/constants/buildings.ts
  - packages/shared/src/constants/ships.ts
  - packages/shared/src/constants/missions.ts
  - packages/shared/src/constants/index.ts
  - packages/bot/package.json
  - packages/bot/tsconfig.json
  - packages/dashboard/package.json
  - packages/dashboard/tsconfig.json
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Monorepo workspace resolves — packages/bot can import from @ogame-bot/shared"
    - "Shared package exports TypeScript types for Planet, Resources, Fleet, Coordinate, Research, BuildingLevels"
    - "Zod schemas validate ogamed response envelope and key endpoint responses"
    - "Game constants include building IDs, ship IDs, and mission type IDs"
    - "TypeScript strict mode compiles without errors across all packages"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package definitions"
      contains: "packages/*"
    - path: "packages/shared/src/types/planet.ts"
      provides: "Planet, Resources, Coordinate type definitions"
      min_lines: 15
    - path: "packages/shared/src/schemas/ogamed.ts"
      provides: "ogamed response envelope Zod schema factory"
      exports: ["ogamedResponseSchema"]
    - path: "packages/shared/src/constants/missions.ts"
      provides: "Mission type ID constants"
      contains: "export"
  key_links:
    - from: "packages/bot/package.json"
      to: "@ogame-bot/shared"
      via: "workspace dependency"
      pattern: '"@ogame-bot/shared": "workspace:\*"'
---

<objective>
Scaffold the pnpm monorepo and build the shared package with OGame domain types, Zod schemas for ogamed responses, and game constants.

Purpose: This creates the foundational structure every other plan depends on — the monorepo workspace, TypeScript configuration, and the shared type system that connects bot logic to ogamed responses.
Output: Working monorepo with three packages; shared package builds and exports types, schemas, and constants.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pnpm monorepo with workspace config, TypeScript, and tooling</name>
  <files>
    pnpm-workspace.yaml, package.json, tsconfig.base.json, .gitignore, .env.example,
    vitest.workspace.ts, packages/shared/package.json, packages/shared/tsconfig.json,
    packages/bot/package.json, packages/bot/tsconfig.json,
    packages/dashboard/package.json, packages/dashboard/tsconfig.json
  </files>
  <read_first>
    - .planning/phases/01-core-infrastructure/01-CONTEXT.md (D-01 monorepo structure, D-02 TS strict + ESM)
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (Standard Stack section for versions and install commands)
  </read_first>
  <action>
    Create the root project structure for a pnpm monorepo with three workspace packages.

    1. **Initialize git** if not already a git repo: `git init`

    2. **Create `pnpm-workspace.yaml`:**
       ```yaml
       packages:
         - 'packages/*'
       ```

    3. **Create root `package.json`:**
       ```json
       {
         "name": "ogame-bot",
         "private": true,
         "type": "module",
         "scripts": {
           "build": "pnpm -r run build",
           "lint": "pnpm -r run lint",
           "typecheck": "pnpm -r run typecheck",
           "test": "vitest run",
           "test:watch": "vitest"
         },
         "devDependencies": {
           "typescript": "^6.0.3",
           "vitest": "^4.1.5",
           "@types/node": "^22.15.0",
           "prettier": "^3.5.3",
           "eslint": "^9.25.0"
         },
         "engines": {
           "node": ">=22.0.0",
           "pnpm": ">=10.0.0"
         }
       }
       ```

    4. **Create `tsconfig.base.json`** per D-02 (strict mode, ESM):
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "Node16",
           "moduleResolution": "Node16",
           "strict": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "forceConsistentCasingInFileNames": true,
           "declaration": true,
           "declarationMap": true,
           "sourceMap": true,
           "outDir": "dist"
         }
       }
       ```

    5. **Create `packages/shared/package.json`:**
       ```json
       {
         "name": "@ogame-bot/shared",
         "version": "0.0.1",
         "type": "module",
         "main": "dist/index.js",
         "types": "dist/index.d.ts",
         "exports": {
           ".": {
             "import": "./dist/index.js",
             "types": "./dist/index.d.ts"
           }
         },
         "scripts": {
           "build": "tsc",
           "typecheck": "tsc --noEmit",
           "lint": "tsc --noEmit"
         },
         "dependencies": {
           "zod": "^4.3.6"
         }
       }
       ```

    6. **Create `packages/shared/tsconfig.json`:**
       ```json
       {
         "extends": "../../tsconfig.base.json",
         "compilerOptions": {
           "rootDir": "src",
           "outDir": "dist"
         },
         "include": ["src"]
       }
       ```

    7. **Create `packages/bot/package.json`:**
       ```json
       {
         "name": "@ogame-bot/bot",
         "version": "0.0.1",
         "type": "module",
         "main": "dist/main.js",
         "scripts": {
           "dev": "tsx watch src/main.ts",
           "build": "tsc",
           "typecheck": "tsc --noEmit",
           "lint": "tsc --noEmit",
           "start": "node dist/main.js"
         },
         "dependencies": {
           "@ogame-bot/shared": "workspace:*",
           "drizzle-orm": "^0.45.2",
           "better-sqlite3": "^12.9.0",
           "ofetch": "^1.5.1",
           "pino": "^10.3.1",
           "js-yaml": "^4.1.1",
           "dotenv": "^17.4.2"
         },
         "devDependencies": {
           "@types/better-sqlite3": "^7.6.13",
           "drizzle-kit": "^0.31.10",
           "pino-pretty": "^13.1.3",
           "tsx": "^4.21.0"
         }
       }
       ```

    8. **Create `packages/bot/tsconfig.json`:**
       ```json
       {
         "extends": "../../tsconfig.base.json",
         "compilerOptions": {
           "rootDir": "src",
           "outDir": "dist"
         },
         "include": ["src"]
       }
       ```

    9. **Create `packages/dashboard/package.json`:**
       ```json
       {
         "name": "@ogame-bot/dashboard",
         "version": "0.0.1",
         "private": true,
         "type": "module",
         "scripts": {
           "build": "echo 'Dashboard placeholder - Phase 5'",
           "typecheck": "echo 'No source yet'",
           "lint": "echo 'No source yet'"
         }
       }
       ```

    10. **Create `packages/dashboard/tsconfig.json`:**
        ```json
        {
          "extends": "../../tsconfig.base.json",
          "compilerOptions": {
            "rootDir": "src",
            "outDir": "dist"
          },
          "include": ["src"]
        }
        ```

    11. **Create `vitest.workspace.ts`:**
        ```typescript
        import { defineWorkspace } from 'vitest/config';

        export default defineWorkspace([
          'packages/shared',
          'packages/bot',
        ]);
        ```

    12. **Create `.gitignore`:**
        ```
        node_modules/
        dist/
        .env
        config.yaml
        data/
        *.db
        *.db-journal
        *.db-wal
        .DS_Store
        ```

    13. **Create `.env.example`** per D-07, D-16:
        ```
        # OGame account credentials
        OGAME_PASSWORD=your_ogame_password_here

        # ogamed connection
        OGAME_OGAMED_URL=http://ogamed:8080

        # ogamed environment (used by ogamed container)
        OGAMED_UNIVERSE=s123-en.ogame.gameforge.com
        OGAMED_USERNAME=your_username
        OGAMED_PASSWORD=your_ogame_password_here
        OGAMED_LANGUAGE=en

        # Optional: Proxy settings
        OGAMED_PROXY=
        OGAMED_PROXY_USERNAME=
        OGAMED_PROXY_PASSWORD=
        OGAMED_PROXY_TYPE=socks5
        ```

    14. **Create placeholder `src` directories** for each package:
        - `packages/shared/src/` (empty, populated in Task 2)
        - `packages/bot/src/` (empty, populated in Plan 02)
        - `packages/dashboard/src/` (empty, placeholder)

    15. **Install dependencies:** Run `pnpm install` from root.

    16. **Verify workspace resolves:** Run `pnpm ls --recursive` — should show all three packages with @ogame-bot/bot depending on @ogame-bot/shared.
  </action>
  <verify>
    <automated>cd /home/simon/codemaxxing/ogame && pnpm ls --recursive 2>&1 | grep -E "(ogame-bot|shared|bot|dashboard)" && pnpm -r exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - pnpm-workspace.yaml exists with `packages/*` pattern
    - Root package.json has type: "module" and devDependencies for typescript, vitest, prettier, eslint
    - tsconfig.base.json has strict: true, module: "Node16", moduleResolution: "Node16"
    - packages/shared/package.json has name "@ogame-bot/shared" with zod dependency
    - packages/bot/package.json has "@ogame-bot/shared": "workspace:*" in dependencies
    - .gitignore includes .env, config.yaml, data/, node_modules/, dist/
    - .env.example exists with OGAME_PASSWORD, OGAMED_UNIVERSE, OGAMED_USERNAME vars
    - `pnpm install` completes without errors
    - `pnpm ls --recursive` shows workspace resolution
  </done>
</task>

<task type="auto">
  <name>Task 2: Build shared package — OGame types, Zod schemas, and game constants</name>
  <files>
    packages/shared/src/index.ts, packages/shared/src/types/planet.ts,
    packages/shared/src/types/fleet.ts, packages/shared/src/types/research.ts,
    packages/shared/src/types/buildings.ts, packages/shared/src/types/index.ts,
    packages/shared/src/schemas/ogamed.ts, packages/shared/src/schemas/planets.ts,
    packages/shared/src/schemas/fleets.ts, packages/shared/src/schemas/research.ts,
    packages/shared/src/schemas/index.ts, packages/shared/src/constants/buildings.ts,
    packages/shared/src/constants/ships.ts, packages/shared/src/constants/missions.ts,
    packages/shared/src/constants/index.ts
  </files>
  <read_first>
    - .planning/phases/01-core-infrastructure/01-CONTEXT.md (D-03 shared exports, D-12 Zod validation)
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (Code Examples section for ogamed response shapes and endpoint documentation)
    - pnpm-workspace.yaml (verify workspace structure from Task 1)
    - packages/shared/package.json (verify zod dependency from Task 1)
  </read_first>
  <action>
    Create all shared type definitions, Zod schemas, and constants. These are the contracts the ogamed client and game state manager will implement against per D-03 and D-12.

    **Types** (`packages/shared/src/types/`):

    1. **`planet.ts`** — Planet and coordinate types matching ogamed GET /bot/planets response:
       ```typescript
       export interface Coordinate {
         galaxy: number;
         system: number;
         position: number;
         type: 'planet' | 'moon';
       }

       export interface Resources {
         metal: number;
         crystal: number;
         deuterium: number;
         energy: number;
         darkmatter?: number;
       }

       export interface Planet {
         id: number;
         name: string;
         coordinate: Coordinate;
         diameter: number;
         fieldsUsed: number;
         fieldsTotal: number;
         temperatureMin: number;
         temperatureMax: number;
         isMoon: boolean;
       }
       ```

    2. **`fleet.ts`** — Fleet and ship types matching ogamed GET /bot/fleets response:
       ```typescript
       export interface Fleet {
         id: number;
         mission: MissionType;
         returnFlight: boolean;
         origin: Coordinate;
         destination: Coordinate;
         arrivalTime: number;  // Unix timestamp
         ships: ShipCount[];
         metal: number;
         crystal: number;
         deuterium: number;
       }

       export interface ShipCount {
         id: ShipId;
         count: number;
       }

       export interface FleetSlots {
         total: number;
         inUse: number;
       }
       ```

    3. **`buildings.ts`** — Building levels per planet:
       ```typescript
       export interface ResourceBuildings {
         metalMine: number;
         crystalMine: number;
         deuteriumSynthesizer: number;
         solarPlant: number;
         fusionReactor: number;
         metalStorage: number;
         crystalStorage: number;
         deuteriumStorage: number;
       }

       export interface Facilities {
         roboticsFactory: number;
         shipyard: number;
         researchLab: number;
         allianceDepot: number;
         missileSilo: number;
         naniteFactory: number;
         terraformer: number;
         spaceDock: number;
       }

       export interface Defence {
         rocketLauncher: number;
         lightLaser: number;
         heavyLaser: number;
         gaussCannon: number;
         ionCannon: number;
         plasmaTurret: number;
         smallShield: number;
         largeShield: number;
         antiBallisticMissile: number;
         interplanetaryMissile: number;
       }

       export interface Ships {
         lightFighter: number;
         heavyFighter: number;
         cruiser: number;
         battleship: number;
         battlecruiser: number;
         bomber: number;
         destroyer: number;
         deathstar: number;
         smallCargo: number;
         largeCargo: number;
         colonyShip: number;
         recycler: number;
         espionageProbe: number;
         solarSatellite: number;
       }
       ```

    4. **`research.ts`** — Research levels:
       ```typescript
       export interface Research {
         energyTechnology: number;
         laserTechnology: number;
         ionTechnology: number;
         hyperspaceTechnology: number;
         plasmaTechnology: number;
         combustionDrive: number;
         impulseDrive: number;
         hyperspaceDrive: number;
         espionageTechnology: number;
         computerTechnology: number;
         astrophysics: number;
         intergalacticResearchNetwork: number;
         gravitonTechnology: number;
         weaponTechnology: number;
         shieldingTechnology: number;
         armourTechnology: number;
       }
       ```

    5. **`types/index.ts`** — Barrel export for all types.

    **Zod Schemas** (`packages/shared/src/schemas/`):

    6. **`ogamed.ts`** — ogamed response envelope schema factory per RESEARCH.md Code Examples. This is the critical schema — every ogamed response has this shape:
       ```typescript
       import { z } from 'zod';

       // Factory that creates a schema for the ogamed response envelope with a typed Result
       export const ogamedResponseSchema = <T extends z.ZodTypeAny>(resultSchema: T) =>
         z.object({
           Status: z.enum(['ok', 'error']),
           Code: z.number(),
           Message: z.string(),
           Result: resultSchema,
         });

       // Type inference helper
       export type OgamedResponse<T> = {
         Status: 'ok' | 'error';
         Code: number;
         Message: string;
         Result: T;
       };

       // Error class for ogamed errors
       export class OgamedError extends Error {
         constructor(
           message: string,
           public readonly code: number,
         ) {
           super(message);
           this.name = 'OgamedError';
         }
       }
       ```

    7. **`planets.ts`** — Zod schemas for planet-related ogamed responses:
       ```typescript
       import { z } from 'zod';

       export const coordinateSchema = z.object({
         galaxy: z.number(),
         system: z.number(),
         position: z.number(),
         type: z.enum(['planet', 'moon']),
       });

       export const resourcesSchema = z.object({
         Metal: z.number().default(0),
         Crystal: z.number().default(0),
         Deuterium: z.number().default(0),
         Energy: z.number().default(0),
         Darkmatter: z.number().optional().default(0),
       });

       // ogamed returns arrays of planet objects with specific keys
       export const planetSchema = z.object({
         ID: z.number(),
         Name: z.string(),
         Coordinate: coordinateSchema,
         Diameter: z.number(),
         FieldsUsed: z.number(),
         FieldsTotal: z.number(),
         TemperatureMin: z.number(),
         TemperatureMax: z.number(),
         IsMoon: z.boolean().default(false),
       });

       export const planetArraySchema = z.array(planetSchema);

       // Resource buildings response
       export const resourceBuildingsSchema = z.object({
         MetalMine: z.number().default(0),
         CrystalMine: z.number().default(0),
         DeuteriumSynthesizer: z.number().default(0),
         SolarPlant: z.number().default(0),
         FusionReactor: z.number().default(0),
         MetalStorage: z.number().default(0),
         CrystalStorage: z.number().default(0),
         DeuteriumStorage: z.number().default(0),
       });

       // Facilities response
       export const facilitiesSchema = z.object({
         RoboticsFactory: z.number().default(0),
         Shipyard: z.number().default(0),
         ResearchLab: z.number().default(0),
         AllianceDepot: z.number().default(0),
         MissileSilo: z.number().default(0),
         NaniteFactory: z.number().default(0),
         Terraformer: z.number().default(0),
         SpaceDock: z.number().default(0),
       });

       // Ships response
       export const shipsSchema = z.object({
         LightFighter: z.number().default(0),
         HeavyFighter: z.number().default(0),
         Cruiser: z.number().default(0),
         Battleship: z.number().default(0),
         Battlecruiser: z.number().default(0),
         Bomber: z.number().default(0),
         Destroyer: z.number().default(0),
         Deathstar: z.number().default(0),
         SmallCargo: z.number().default(0),
         LargeCargo: z.number().default(0),
         ColonyShip: z.number().default(0),
         Recycler: z.number().default(0),
         EspionageProbe: z.number().default(0),
         SolarSatellite: z.number().default(0),
       });

       // Defence response
       export const defenceSchema = z.object({
         RocketLauncher: z.number().default(0),
         LightLaser: z.number().default(0),
         HeavyLaser: z.number().default(0),
         GaussCannon: z.number().default(0),
         IonCannon: z.number().default(0),
         PlasmaTurret: z.number().default(0),
         SmallShield: z.number().default(0),
         LargeShield: z.number().default(0),
         AntiBallisticMissile: z.number().default(0),
         InterplanetaryMissile: z.number().default(0),
       });
       ```

       IMPORTANT: Use `.default(0)` on numeric fields per Pitfall 5 in RESEARCH.md — ogamed responses may have missing fields when game updates break parsers. Also use `.passthrough()` initially to allow undocumented fields.

    8. **`fleets.ts`** — Zod schema for fleet data:
       ```typescript
       import { z } from 'zod';
       import { coordinateSchema } from './planets.js';

       const shipCountSchema = z.object({
         ID: z.number(),
         Number: z.number().default(0),
       });

       export const fleetSchema = z.object({
         ID: z.number(),
         Mission: z.number(),
         ReturnFlight: z.boolean().default(false),
         Origin: coordinateSchema,
         Destination: coordinateSchema,
         ArrivalTime: z.number(),
         Ships: z.array(shipCountSchema).default([]),
         Metal: z.number().default(0),
         Crystal: z.number().default(0),
         Deuterium: z.number().default(0),
       });

       export const fleetArraySchema = z.array(fleetSchema);
       ```

    9. **`research.ts`** — Zod schema for research response:
       ```typescript
       import { z } from 'zod';

       export const researchSchema = z.object({
         EnergyTechnology: z.number().default(0),
         LaserTechnology: z.number().default(0),
         IonTechnology: z.number().default(0),
         HyperspaceTechnology: z.number().default(0),
         PlasmaTechnology: z.number().default(0),
         CombustionDrive: z.number().default(0),
         ImpulseDrive: z.number().default(0),
         HyperspaceDrive: z.number().default(0),
         EspionageTechnology: z.number().default(0),
         ComputerTechnology: z.number().default(0),
         Astrophysics: z.number().default(0),
         IntergalacticResearchNetwork: z.number().default(0),
         GravitonTechnology: z.number().default(0),
         WeaponTechnology: z.number().default(0),
         ShieldingTechnology: z.number().default(0),
         ArmourTechnology: z.number().default(0),
       });
       ```

    10. **`schemas/index.ts`** — Barrel export for all schemas.

    **Constants** (`packages/shared/src/constants/`):

    11. **`missions.ts`** — Mission type IDs (from OGame protocol):
        ```typescript
        export const MISSION_TYPE = {
          ATTACK: 1,
          UNION_ATTACK: 2,
          DEPLOY: 3,
          TRANSPORT: 4,
          UNION_TRANSPORT: 5,
          RELOCATE: 6,
          STATION: 7,
          ESPIONAGE: 6,
          COLONIZE: 7,
          HARVEST: 8,
          EXPEDITION: 15,
        } as const;

        export type MissionType = (typeof MISSION_TYPE)[keyof typeof MISSION_TYPE];
        ```

        NOTE: OGame mission IDs — verify against ogamed source if values look wrong during integration testing.

    12. **`buildings.ts`** — Building ID constants:
        ```typescript
        export const BUILDING_ID = {
          METAL_MINE: 1,
          CRYSTAL_MINE: 2,
          DEUTERIUM_SYNTHESIZER: 3,
          SOLAR_PLANT: 4,
          FUSION_REACTOR: 12,
          METAL_STORAGE: 22,
          CRYSTAL_STORAGE: 23,
          DEUTERIUM_STORAGE: 24,
          ROBOTICS_FACTORY: 14,
          SHIPYARD: 21,
          RESEARCH_LAB: 31,
          ALLIANCE_DEPOT: 34,
          MISSILE_SILO: 44,
          NANITE_FACTORY: 15,
          TERRAFORMER: 33,
          SPACE_DOCK: 36,
        } as const;

        export type BuildingId = (typeof BUILDING_ID)[keyof typeof BUILDING_ID];
        ```

    13. **`ships.ts`** — Ship ID constants:
        ```typescript
        export const SHIP_ID = {
          SMALL_CARGO: 202,
          LARGE_CARGO: 203,
          LIGHT_FIGHTER: 204,
          HEAVY_FIGHTER: 205,
          CRUISER: 206,
          BATTLESHIP: 207,
          COLONY_SHIP: 208,
          RECYCLER: 209,
          ESPIONAGE_PROBE: 210,
          BOMBER: 211,
          SOLAR_SATELLITE: 212,
          DESTROYER: 213,
          DEATHSTAR: 214,
          BATTLECRUISER: 215,
        } as const;

        export type ShipId = (typeof SHIP_ID)[keyof typeof SHIP_ID];
        ```

    14. **`constants/index.ts`** — Barrel export for all constants.

    15. **Root barrel** `packages/shared/src/index.ts`:
        ```typescript
        export * from './types/index.js';
        export * from './schemas/index.js';
        export * from './constants/index.js';
        ```

    16. **Build and verify:** Run `pnpm --filter @ogame-bot/shared build` and verify TypeScript compiles without errors.
  </action>
  <verify>
    <automated>cd /home/simon/codemaxxing/ogame && pnpm --filter @ogame-bot/shared build 2>&1 && node -e "const s = await import('./packages/shared/dist/index.js'); console.log('Exports:', Object.keys(s).length > 0 ? 'OK (' + Object.keys(s).length + ')' : 'FAIL')" 2>&1</automated>
  </verify>
  <done>
    - packages/shared/src/types/ exports: Coordinate, Resources, Planet, Fleet, ShipCount, FleetSlots, ResourceBuildings, Facilities, Defence, Ships, Research
    - packages/shared/src/schemas/ exports: ogamedResponseSchema, OgamedError, planetSchema, planetArraySchema, resourcesSchema, resourceBuildingsSchema, facilitiesSchema, shipsSchema, defenceSchema, fleetSchema, fleetArraySchema, researchSchema
    - packages/shared/src/constants/ exports: MISSION_TYPE, BUILDING_ID, SHIP_ID, MissionType, BuildingId, ShipId
    - `pnpm --filter @ogame-bot/shared build` compiles with zero errors
    - `dist/index.js` can be imported and exports are accessible
    - All Zod numeric fields use `.default(0)` to handle missing fields gracefully
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → source code | Trusted — developer writing types and schemas |
| Build system → TypeScript compiler | Trusted — standard compilation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E | TypeScript strict mode | mitigate | Strict null checks catch type mismatches at compile time |
| T-01-02 | T | Zod schemas | mitigate | Default values prevent undefined access when ogamed fields are missing |

This plan creates only type definitions and schemas — no runtime trust boundaries yet. Security enforcement begins in Plan 02 (config secrets handling, API communication).
</threat_model>

<verification>
1. `pnpm install` completes without errors
2. `pnpm --filter @ogame-bot/shared build` compiles with zero errors
3. `pnpm --filter @ogame-bot/shared typecheck` passes
4. Shared dist/index.js exports all types, schemas, and constants
5. Workspace resolution works: `pnpm ls --recursive` shows bot → shared dependency
</verification>

<success_criteria>
- Monorepo scaffolded with pnpm workspaces, strict TypeScript, ESM modules
- Shared package builds and exports: 11+ types, 12+ Zod schemas, 3 constant objects
- Bot package has workspace dependency on shared
- `.gitignore` prevents committing secrets and local data
- `.env.example` documents required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>
